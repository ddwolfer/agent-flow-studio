mcp[cli]>=1.0
yt-dlp>=2024.9.0
requests>=2.31
feedparser>=6.0
httpx>=0.27
readability-lxml>=0.8.1
markdownify>=0.13
# yfinance major-version bumps have broken us before (0.2.51→1.x rejected the
# session shim from studio era — see commit 5d8b77b + KG f83beb66). Pin to 1.x.
yfinance>=1.3,<2
# curl_cffi 0.x is pre-1.0 — minor versions can be breaking. Allow 0.15+ but
# block jumps past 0.x major to keep yfinance compat predictable.
curl_cffi>=0.15,<1
# Local ASR for caption-less YouTube audio (replaces the ollama/gemma path,
# which silently ignored audio on the OpenAI-compat endpoint — see commit
# swapping gemma_transcribe → whisper_transcribe). CPU int8, auto language.
faster-whisper>=1.0
pytest>=8.0
