node_modules/
mcp/knowledge-graph/node_modules/
mcp/knowledge-graph/*.db
mcp/knowledge-graph/*.sqlite*
.agent-bridge/
.agent-bridge-markdown/
*.backup
.git.bak-*
.DS_Store
.claude/scheduled_tasks.lock
/package-lock.json

# Agent Bridge (auto-added by let-them-talk init)
.mcp.json
.codex/
.gemini/

# Brainstorming visual-companion scratch
.superpowers/

# Project-specific (agent-flow-studio)
# Secrets — never commit .env files anywhere in the tree
**/.env
**/.env.local
**/.env.*.local
# Browser cookie exports (YouTube session — account credential, never commit)
**/youtube-cookies.txt
**/*cookies*.txt
# Generated daily reports (HTML/PDF/history) — user-specific output, never commit
finance-workflows/reports/
